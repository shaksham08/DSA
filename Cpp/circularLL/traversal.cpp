#include <bits/stdc++.h>
using namespace std;

struct Node
{
    int data;
    Node *next;
    Node(int x)
    {
        data = x;
        next = NULL;
    }
}

main(int argc, char const *argv[])
{
    Node *head = new Node();

    return 0;
}
